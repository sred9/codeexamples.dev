---
title: How to find length of a string in JavaScript
date: "2019-12-15"
tags: ['javascript','js','strings']
template: post
---

To find the length of a string we need to use the `string.length` property in JavaScript.

Example:

```js
const str = "ocean";

console.log(str.length); // 5
```

The `length` property returns the number of characters in a string.

> length property is read-only.
